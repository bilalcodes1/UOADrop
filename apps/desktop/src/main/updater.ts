/**
 * Auto-Update stub — ready to activate once code signing is in place.
 *
 * To enable:
 * 1. Add `electron-updater` to dependencies
 * 2. Configure `publish` in electron-builder config (GitHub Releases)
 * 3. Sign the app (Apple Developer / Windows Authenticode)
 * 4. Call `initAutoUpdater()` from index.ts
 *
 * For now this module is a no-op placeholder.
 */

export function initAutoUpdater(): void {
  // eslint-disable-next-line no-console
  console.log('[UOADrop] Auto-updater is disabled (unsigned build).');
}
