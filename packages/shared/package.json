{
  "name": "@uoadrop/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types, constants, zod schemas, utils across web + desktop + edge functions",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts",
    "./constants": "./src/constants.ts"
  },
  "scripts": {
    "lint": "echo 'TODO: eslint' ",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
