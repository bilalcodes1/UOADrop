{
  "name": "@uoadrop/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Shared React UI components (shadcn/ui based) for web + desktop renderer",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo 'TODO: eslint' ",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
